<%= form_with(model: airplane, local: true) do |form| %>
  <% if airplane.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(airplane.errors.count, "error") %> prohibited this airplane from being saved:</h2>

      <ul>
      <% airplane.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"Airplane model:" %>
    <%= form.text_area :plane_model %>
    <%= form.label :"Rows:" %>
    <%= form.text_area :rows %>
    <%= form.label :"Columns:" %>
    <%= form.text_area :columns %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
